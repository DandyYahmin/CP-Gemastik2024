#include <iostream>
using namespace std;

int main() {
    // unsigned int N, K;
    // cin >> N >> K;

    // unsigned int A[N] = {};
    // for (int i = 0; i < N; i++) {
    //     cin >> A[i];
    // }

    // unsigned int fimoracci = 0;

    // for (int i = 2; i < N; i++) {
    //     for (int j = 1; j < N; j++) {
    //         for (int k = 0; k < N; k++) {
    //             if (A[i] >= (A[j] + A[k])) fimoracci++;
    //         }
    //     }
    // }

    // cout << fimoracci << endl;

    return 0;
}